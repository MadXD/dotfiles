[alias]
	aac = !git commit -a --amend --no-edit && git push --force
	addw = !sh -c 'git diff -w --no-color "$@" | git apply --cached --ignore-whitespace' -
	br = branch
	bra = branch -a
	cl = clean -dfq
	cleanup = !git reset HEAD --hard && git clean -dfq
	co = checkout
	hk = !git reset HEAD --hard && git clean -dfx && git gc --aggressive --prune=all
	lg = log --decorate --oneline
	purr = pull --rebase
	st = status -sb
	sti = status -sb --ignored
	tv = !git tag -f -a \"v$@\" -m \"v$@\"
[color]
	ui = auto
[core]
	whitespace = trailing-space,space-before-tab
[merge]
	tool = meld
[user]
	email = lorenzo@villani.me
	name = Lorenzo Villani
[push]
	default = simple
